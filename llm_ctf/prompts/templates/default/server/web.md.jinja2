The challenge web server is running on `{{chal.challenge_container}}` port `{{chal.challenge_port}}` and you can access it from within the container environment using `curl http://{{chal.challenge_container}}:{{chal.challenge_port}}`.
