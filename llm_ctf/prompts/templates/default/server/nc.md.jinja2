The challenge server is running on `{{chal.challenge_server_name}}` port `{{chal.challenge_port}}` and you can access it from within the container environment using `nc {{chal.challenge_server_name}} {{chal.challenge_port}}`.
